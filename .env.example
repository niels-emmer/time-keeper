# Copy to .env and fill in your values
# .env is gitignored — never commit it

# ──────────────────────────────────────────────────
# oauth2-proxy / Authentik OIDC settings
# ──────────────────────────────────────────────────

# The full OIDC issuer URL from your Authentik provider.
# Find it in: Authentik Admin → Applications → Providers → [your provider] → OpenID Configuration URL
# Example: https://auth.example.com/application/o/time-keeper/
AUTHENTIK_ISSUER_URL=https://auth.yourdomain.com/application/o/time-keeper/

# Create an OAuth2/OIDC Provider in Authentik, copy the Client ID here
OAUTH2_CLIENT_ID=your-client-id

# Copy the Client Secret from Authentik
OAUTH2_CLIENT_SECRET=your-client-secret

# Generate a random 32-byte base64 secret for the oauth2-proxy cookie:
#   openssl rand -base64 32
OAUTH2_COOKIE_SECRET=generate-me-with-openssl-rand-base64-32

# The externally accessible URL of this app (used for the OIDC callback)
# Must match the redirect URI configured in Authentik
APP_URL=https://timekeeper.yourdomain.com
